using UnityEngine;

namespace Environment
{
    public class Snow : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
            // TODO implement different levels of snowfall
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
